query EventSchema($rowId: UUID!) {
  eventSchema(rowId: $rowId) {
    rowId
    name
    source
    description
    payloadSchema
    enforcement
    version
    compatibilityMode
    previousVersionId
    migrationTransform
    createdAt
    updatedAt
  }
}
